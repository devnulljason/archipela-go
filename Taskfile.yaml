# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  build:
    cmd: go build -o build/pela cmd/pela.go

  doc:
    cmd: go doc -http .

  fmt:
    cmd: golangci-lint fmt --diff-colored

  lint:
    cmd: golangci-lint run

  run:
    deps: [build]
    dir: build
    cmd: ./pela

  test:
    cmd: go test -v -cover ./...

  vuln:
    cmd: govulncheck -show color ./...
